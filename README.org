#+title: emacsreach-client

Client script to extend the reach of Emacs across permission and remote access boundaries. 

Install as `edit` or `er` (or some other convenient shorthand) on your local machine and any remotes you choose. Set `MACHINE` in your env to provide automatic TRAMP prefixing. 
